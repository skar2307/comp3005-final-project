<%= form_with(model: health_metric) do |form| %>
  <% if health_metric.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(health_metric.errors.count, "error") %> prohibited this health_metric from being saved:</h2>

      <ul>
        <% health_metric.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :value, style: "display: block" %>
    <%= form.number_field :value %>
  </div>

  <div>
    <%= form.label :members_id, style: "display: block" %>
    <%= form.text_field :members_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
